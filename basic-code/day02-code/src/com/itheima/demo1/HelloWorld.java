package com.itheima.demo1;

public class HelloWorld {
    public static void main(String[] args) {

        // int[ ] array = new int[]{ };
        // int[ ] array = { };
        double[] array6 = {1.2, 3.2, 7.9};
        System.out.println(array6); //打印出来的是地址：[D@36baf30c
        // [ 表示是个数组
        // D 表示是double
        //  @ 表示一个间隔符号
        //  36baf30c （十六进制）

        //数组默认初始化值：
        //整数类型： 0
        //小数类型： 0.0
        // 字符 '/u0000'  空格
        // 布尔 false

        /*int[ ] arr = new int[10];
        Random r = new Random();

        for(int i = 0; i < arr.length; i++)
        {
            int number = r.nextInt(100) + 1;
            arr[i] = number;
        }

       int sum = 0;
        for (int i = 0; i < arr.length; i++) {
            sum = sum + arr[i];
        }
        System.out.println(sum);*/







    }
}
